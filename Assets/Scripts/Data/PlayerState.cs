namespace BeerRun
{
    /// <summary>
    /// Enum representing the different states a player can be in
    /// </summary>
    public enum PlayerState
    {
        Idle,
        Running,
        Jumping,
        Falling,
        Injured,
        Dead
    }
}