namespace BeerRun
{
    /// <summary>
    /// Enum representing different types of platforms in the level
    /// </summary>
    public enum PlatformType
    {
        Ground,
        Platform,
        OneWayPlatform,
        MovingPlatform
    }
}